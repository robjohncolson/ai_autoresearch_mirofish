# Agents

This project does not currently use multi-agent dispatch.
The previous agent manifest (for the AP Stats synthetic student project) is
preserved in git history at commit `67b0121`.

Future: the autoresearch orchestrator (`scripts/autoresearch.py`) will act as
a single-agent loop — no parallel dispatch needed.
